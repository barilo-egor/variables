springdoc:
  swagger-ui:
    enabled: "true"
    path: "/swagger"
  api-docs:
    path: "/swagger/api-docs"
server:
  port:
spring:
  datasource:
    username: ""
    password: ""
    url: ""
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: update
  grpc:
    server:
      port:
  kafka:
    bootstrap-servers: ""
kafka:
  topic:
    bulk-discount: "test-event-discount-update"
variables:
  bulk-discount:
    send-all-after-start: true