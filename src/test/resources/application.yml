server:
  port:
spring:
  datasource:
    username: ""
    password: ""
    url: ""
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: update
  grpc:
    server:
      port: 9090
  kafka:
    bootstrap-servers: ""
kafka:
  topic:
    bulk-discount: "test-event-discount-update"
variables:
  bulk-discount:
    send-all-after-start: true