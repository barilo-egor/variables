spring:
  port:
  datasource:
    username:
    password:
    url:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto:
  grpc:
    server:
      host:
      port:
  kafka:
    bootstrap-servers:
kafka:
  topic:
    bulk-discount:
    review-prize:
variables:
  bulk-discount:
    send-all-after-start: true
  review-prize:
    send-all-after-start: