spring:
  port:
  datasource:
    username:
    password:
    url:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto:
  grpc:
    server:
      port:
  kafka:
    bootstrap-servers:
kafka:
  topic:
    bulk-discount:
variables:
  bulk-discount:
    send-all-after-start: true